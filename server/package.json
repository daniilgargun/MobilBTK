{
  "name": "btk-schedule-watcher",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: следит за страницей расписания БТК и будит приложение через FCM",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit && tsc --noEmit -p tsconfig.test.json",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260906.1",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.129.0"
  }
}
