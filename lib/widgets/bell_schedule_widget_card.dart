import 'package:flutter/material.dart';

class BellScheduleWidgetCard extends StatelessWidget {
  const BellScheduleWidgetCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}